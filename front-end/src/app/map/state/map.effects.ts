import {Injectable} from '@angular/core';

@Injectable()
export class MapEffects {
  // constructor(
  //   private actions$: Actions,
  //   private mapService: MapService
  // ) {}
  //
  // enableDrawing$ = createEffect(() =>
  //   this.actions$.pipe(
  //     ofType(mapActions.enableSelection),
  //     map(() => this.mapService.enableDrawing())
  //   ), { dispatch: false }
  // );
  //
  // disableDrawing$ = createEffect(() =>
  //   this.actions$.pipe(
  //     ofType(mapActions.disableSelection),
  //     map(() => this.mapService.disableDrawing())
  //   ), { dispatch: false }
  // );
  //
  // clearSelection$ = createEffect(() =>
  //   this.actions$.pipe(
  //     ofType(mapActions.clearSelection),
  //     map(() => this.mapService.clearSelection())
  //   ), { dispatch: false }
  // );
  //
  // toggleRegionLayer$ = createEffect(() =>
  //   this.actions$.pipe(
  //     ofType(mapActions.toggleRegionLayer),
  //     map(() => this.mapService.toggleLayer('region'))
  //   ), { dispatch: false }
  // );
  //
  // toggleRegionSelectionLayer$ = createEffect(() =>
  //   this.actions$.pipe(
  //     ofType(mapActions.toggleRegionSelectionLayer),
  //     map(() => this.mapService.toggleLayer('region-selection'))
  //   ), { dispatch: false }
  // );
  //
  // toggleLocationLayer$ = createEffect(() =>
  //   this.actions$.pipe(
  //     ofType(mapActions.toggleLocationLayer),
  //     map(() => this.mapService.toggleLayer('location'))
  //   ), { dispatch: false }
  // );
  //
  // toggleAllocationLayer$ = createEffect(() =>
  //   this.actions$.pipe(
  //     ofType(mapActions.toggleAllocationLayer),
  //     map(() => this.mapService.toggleLayer('allocation'))
  //   ), { dispatch: false }
  // );
}
